mixin loginForm()
  h5 Login
  a(href='/register') This login is only for registered site members. If you are insterested in becoming a member to contribute your work or review submissions click here!
  form.form(action="/login" method="POST")
    label(for="email") Email Address
    input(type="email" name="email")
    br
    label(for="password") Password
    input(type="password" name="password")
    br
    input.button.btn.btn-primary(type="submit" value="Log In →" id="submitbutton")

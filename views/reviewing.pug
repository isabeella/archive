extends layout

include mixins/_reviewArticleForm

block content
    .documentandtitle
        embed(src=`/uploads/${article.file}` || `/uploads/NOFILEFOUND.pdf` width="100%" height="800px")
    .reviewingform
        h5 Reviewing
        +reviewArticleForm()